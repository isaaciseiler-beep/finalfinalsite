// components/ExperienceEducation.tsx — FULL FILE
"use client";

export default function ExperienceEducation() {
  // kept for reuse within popup; minimal since popup structures content
  return (
    <div className="space-y-4 p-4 text-sm text-muted">
      <div>education content is rendered in the popup.</div>
    </div>
  );
}
